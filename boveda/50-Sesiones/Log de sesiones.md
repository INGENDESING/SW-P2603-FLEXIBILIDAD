# Log de sesiones

Historial cronológico para continuidad entre sesiones. **Al cerrar cada sesión, agregar una entrada** (fecha, qué se hizo, dónde quedó, próximo paso).

## Protocolo de carga

Al iniciar sesión con «ejecuta inicializa», leer en este orden:

1. ✅ `boveda/00-Inicio.md` (mapa)
2. ✅ `boveda/60-Pendientes/Pendientes y bloqueos.md` (qué sigue)
3. ✅ `boveda/20-Lineas/Resumen de líneas.md` (tabla verdad)
4. ✅ Esta nota (últimas entradas)
5. ✅ `contexto.md` y `task/todo.md` (detalle completo)
6. Bajo demanda: notas individuales de línea, [[30-Decisiones/Decisiones y lecciones]], [[40-Workflows/Comandos y pipelines]], [[10-Proyecto/Bases de diseño congeladas]]

Luego reportar al usuario: estado global, última tarea, pendientes y próximo paso propuesto.

## Entradas

### 2026-09-22 (sesión 2, tarde)
- **PCF120 RESCATADO (plan B aprobado y ejecutado T1–T4)**: la investigación mostró que la "coordenada basura" (-9776646, -9840066, -102887, bore 0.0) es centinela de Plant 3D para componentes sin geometría (57 bloques, volcados como primer bloque de cada sección) y que gaskets/pernos/welds venían **duplicados 58×** (una copia por sección; 1336→25, 1162→22, 148→91) — la pauta inicial de "hardware vecino" era un espejismo de esa duplicación. Rescate determinista con `scripts/fix_pcf120_basura.py` (nuevo; `--proponer`/`--aplicar`/`--verificar`): emparejamiento de los 24 stub-ends reales (colineales, enfrentados, mismo bore) → 11 uniones gemelas + unión con la knife real + boquilla de bomba; FF = gap − 6.35 mm identifica cada válvula (knife 3" 50.96 → PMVAL1-084; V-port 165.1 → I30RF01K01-V1; check 3" ×2 73.0; check 6" 98.4; butterflies 42.0/42.0/46.9/52.0/57.3). Recuperado el **MAGNETIC FLOW METER 4"** (INSTRUMENT que `fix_pcf.py` había eliminado por no tener END-POINTs). Ramal 2" completo: bola THD en hueco 91.8, 2× ELL 45 SW en huecos 46.9 (CENTRE-POINT = intersección de tangentes, ANGLE corregido 9000→4500), sockolet en riser 6" (2º sockolet eliminado, no resoluble). Eliminadas 24 bridas basura + 18 soportes (lista en `PCF120 - SOPORTES ELIMINADOS.txt`). Checkpoint T2: el usuario aprobó con criterio "geometría y diámetros correctos; TAGs y detalles los ajusto en CAESAR".
- **Verificación 5/5 OK** (probado primero en copia): 0 coordenadas basura; conteos FLANGE=24/VALVE=12/INSTRUMENT=1/ELBOW=30/OLET=1/SUPPORT=11; 0 bores incoherentes; 24 FLWN correctas; 5 extremos sueltos = extremos de línea preexistentes (idénticos en el respaldo). PCF120.pcf: 1.13 MB → 126 KB. Respaldo `PCF120 - ANTES RESCATE.pcf`.
- **Quedó en**: el usuario importa PCF120 en CAESAR, crea .C2 y corre; en CAESAR ajusta TAGs de butterflies/checks (asignados por tamaño en orden de documento), repone los 18 soportes y revisa sockolet 2 + artefacto 1" (0.76, -528.48, 5356.57). Pendientes de siempre: tifs SIM-007 → DOCS-182, transmittal, fechas de firma, AnexoResultado, corridas 12.0/13.0/14.0/15.0, verificación presión 180 vs ~150 psi, validación cliente. NO se hizo commit (regla: solo con confirmación explícita).

### 2026-09-22
- **PCF120 corregido con `fix_pcf.py` (carpeta 15.0 — Descarga bomba PP30BT17, línea nueva del usuario)**: respaldo `PCF120 - ORIGINAL PLANT3D.pcf`; transformaciones: 2720 END-POSITION-* (PCF mucho más grande que los anteriores: 30346 líneas, ~2719 ITEM-CODE, 1336 gaskets, 1162 sets de pernos), 1 instrumento longitud cero, 0 pares stub/brida, 0 TAP. **Con AVISOS**: 24 stub-end sin brida pareja (coordenadas reales, distintas) y 22 bridas de longitud cero sin fusionar en una coordenada basura `(-9776646.0, -9840066.0, -102887.0)` (~98 ocurrencias en el archivo) — **pendiente investigar**: probablemente ítems de Plant 3D sin ubicación válida (sesión interrumpida antes de completar el análisis).
- **Material verificado en las descripciones**: tubería A312 TP304L Sch 10S ✓, accesorios BW A403 WP304L Sch 10S ✓, sockolets/ells SW A182 F304L ✓, válvulas CF8M. ⚠️ **Hallazgo**: las 24 bridas LJ son **ASTM A182 Gr F304 (sin L)** — distinto del F304L de los PCF anteriores; verificar contra isométrico `PCF120.png`. Espec nueva `SSS3` (no SS10S); NO se detectó tramo Sch 40S.
- **Quedó en**: PCF120 listo salvo los dos avisos por investigar (coordenada basura; stub-ends sin pareja — en PCFs anteriores los pares se fusionaban, aquí 0 pares y 24 stub-ends sueltos). Próxima sesión: investigar los ítems en la coordenada basura, luego el usuario importa en CAESAR, crea `.C2` y corre. Pendientes de siempre: tifs SIM-007 → DOCS-182, transmittal, fechas de firma, AnexoResultado, corridas 12.0/13.0/14.0/15.0, verificación presión 180 vs ~150 psi, validación cliente.

### 2026-09-17
- **PCF118 corregido con `fix_pcf.py` (carpeta 14.0 — Descarga bomba PP30BT07, línea nueva del usuario)**: respaldo `PCF118 - ORIGINAL PLANT3D.pcf`; transformaciones: 10 END-POSITION-*, 5 INSTRUMENT-DIAL, 2 instrumentos longitud cero, 46 pares stub-end/brida, 27 TAP-CONNECTION; sin advertencias. Material verificado: A312 TP304L / A403 WP304L / A182 F304L, Sch 10S ✓. ⚠️ **Hallazgo**: tramo en Sch 40S (spec `SS40S` en encabezado y algunos componentes) — mismo caso que PCF114 (SIM-015); verificar contra isométrico `PCF118.png` al correr. Pendiente: importar en CAESAR II 2019, crear `.C2` (convención `P2603-PR-SIM-XXX.C2`, siguiente SIM libre) y correr.
- **Quedó en**: PCF118 listo para importar. Próxima sesión: «ejecuta inicializa». Pendientes de siempre: tifs SIM-007 → DOCS-182, verificación presión 180 vs ~150 psi en .C2, fechas de firma + `\ead{}`, transmittal a C. Muriel, AnexoResultado, corridas 12.0/13.0/14.0, validación cliente.

### 2026-09-11
- **Sesión iniciada con desvío**: el usuario planteó un plan sobre carga de horas por disciplina de una aplicación de control de horas DML; tras 3 iteraciones de aclaración el plan quedó aprobado y se redactó el procedimiento… pero el usuario detectó que correspondía a OTRO proyecto → **todo deshecho** (archivo eliminado, `task/todo.md` restaurado a 562 líneas). Lección: confirmar el proyecto antes de ejecutar planes transversales.
- **Reorganización de archivos y carpetas (plan aprobado, T1–T8)**: (1) carpetas de línea renombradas a prefijo de dos dígitos `01.0`–`09.0` (10.0–13.0 ya estaban) → orden alfabético correcto y el JSON del dashboard queda en orden SIM-002→015; (2) `fix_pcf.py`→`scripts/`, `logo1.png`→`assets/logo1.png` con ajuste de `copy_logo` en `parse_caesar_md.py`; (3) artefactos SIM-015 (`.c2db`/`.XML`/`-x.wrn`) movidos de 10.0 a 11.0; (4) `.tmp-playwright/` eliminado; (5) `.gitignore` ampliado con scratch CAESAR (CONTROLU/OCONTROLU/DBGENBIN/COMNDINP/TEMPMAT*/`*.c2db`/`*.XML`) — siguen en disco, fuera de git; (6) triggers del workflow reemplazados por `'**/P2603-PR-*.md'` + `'**/Graficas/**'` (los patrones viejos con nombres sin prefijo ya no coincidían con las carpetas prefijadas); (7) referencias vivas actualizadas (`contexto.md`, tabla verdad, `Comandos y pipelines`, pendientes). **Verificación**: parser regenerado — contenido de las 11 líneas IDÉNTICO por `id` (solo cambian nombre/carpeta y orden); `convertir_graficas.py` idempotente (0 nuevas, 45 al día); `git check-ignore` confirma scratch excluido.
- **Commit+push acumulado AUTORIZADO y ejecutado**: `ae3021f` en `main` (977 A / 44 R / 23 D / 9 M — incluye todo el trabajo TP304L del 09-09/09-10, transmittal y la reorganización). Workflow "Update Flexibilidad Dashboard" **success** (update-dashboard + deploy-pages) → sitio público actualizado con las 11 líneas. Único aviso: Node.js 20 deprecado en actions (mantenimiento futuro del workflow, sin impacto).
- **Quedó en**: todo publicado y repo sincronizado. Próxima sesión: «ejecuta inicializa». Pendientes del usuario: re-exportar tifs de SIM-007 (03.0) → convertir + parser + DOCS-182; número real del transmittal y envío a C. Muriel; fechas de revisión/aprobación y correo `\ead{}` en los 11 informes; instrucción AnexoResultado; corridas SIM-016 (12.0)/PCF115 (13.0); validación cliente.

### 2026-09-10
- **Auditoría de admisibles en los .md (2 rondas)**: se confirmó que SIM-007 (3.0) y SIM-011 (7.0) seguían con material TP304 (137895.1 kPa = 20 ksi) en sus corridas del 09-09 — SIM-007 en elementos 75→100 (admisible EXP 325881.3 kPa, imposible con TP304L cuyo techo es 287856) y SIM-011 en casi todo el modelo. El usuario recorrió las 3 líneas afectadas: **SIM-010** (09:03 → 46.7 % @430 EXP, resuelve el 96.8 %), **SIM-007** (14:42 → 70.1 % @100 EXP caso 9, 0×137895.1) y **SIM-011** (15:12 → 67.2 % @260 Alt-SUS, valor exacto al calculado). **11/11 líneas limpias con TP304L, todas PASSED.** Ratio máximo del proyecto: 70.1 % (SIM-007). OJO: los tifs de `Graficas/` de SIM-007 son de la corrida vieja (los esfuerzos SÍ cambiaron) → re-exportar; los de SIM-011 siguen válidos (solo cambió el admisible).
- **Plan TP304L (aprobado el 09-09) EJECUTADO T0–T4**: T1: `scripts/convertir_graficas.py` nuevo (tif→png, fondo blanco, excluye AnexoResultado; 45 PNG); `parse_caesar_md.py` y `extraer_informe.py` actualizados (leen `Graficas/`, excluyen `_corrida_anterior`, isométrico también desde `Graficas/PCF*`; esquema de assets `graf_desplazamiento[_N]`/`graf_stress_percent[_N]`/`graf_nodos_soporte[_N]`). T2: JSON verificado 11/11 contra tabla verdad. T3 (swarm 11 agentes): **9 informes actualizados (DOCS-180→188) + 2 nuevos (DOCS-189 SIM-014, 24 pág.; DOCS-190 SIM-015, 28 pág.)**, todos compilados, 0 refs indefinidas, números == .md, verificación visual OK. Páginas: 23/22/23/23/22/27/28/24/22/24/28. T4: dashboard `lineas.json` con 11 líneas, 0 referencias rotas de assets.
- **Desviaciones / notas**: conversiones a `longtable` de `tab_esfuerzos_critico.tex` hechas a mano en SIM-010/011/012/015 (se pierden si se re-ejecuta `extraer_informe.py` → mejora futura del script); el usuario renombró el .md con espacio de 1.0; SIM-015 declara tramo Sch 40S y densidad 1030 kg/m³ (como SIM-014); huérfanos en `dashboard/assets/graficos/` (SIM-002/003/007/011.png, sin referencia) y assets viejos sin referencia en algunos informes (no eliminados por cambios mínimos).
- **Transmittal HTML (2026-09-10)**: `transmittal/transmittal_DOCS-180_a_DOCS-190.html` — correo de envío al Ing. Cristian Muriel para revisión de calidad (formatos, estructura, codificación); paleta DML (#117341 extraída del logo), tablas translúcidas, logos DML/Smurfit embebidos en base64, relación de los 11 informes (código, título, R0, elab. 01/09/2026, páginas, PDF), nota de fechas de revisión/aprobación pendientes. N.° de transmittal `[TR-001]` es placeholder → confirmar codificación con el usuario. Verificado con render headless Chrome.
- **Quedó en**: memoria actualizada. Pendientes: commit+push (despliega dashboard 11 líneas), re-exportar tifs de SIM-007, fechas de firma/correo en los 11 informes (elab. 01/09/2026 ya aplicada; revisión/aprobación pendientes), AnexoResultado EN ESPERA, corridas SIM-016 (12.0)/PCF115 (13.0), validación cliente. NO se hizo commit (regla: solo con confirmación explícita).

### 2026-09-09
- **Recorridas TP304L recibidas para las 11 líneas (carpetas 1.0–11.0)**: `.OUT`/`.md` nuevos (2026-09-09) + carpetas `Graficas/` con imágenes por tipo: `DesplazamientoN` (desplaz. máximos del caso crítico de desplazamiento → §9.1), `NodosSoporteN` (vistas con nodos donde hay soportes → §9.3), `StressPercentN` (mapa de color del mayor esfuerzo, caso crítico → §9.2), `AnexoResultado` (resumen gráfico para anexos — **EN ESPERA de instrucciones, no procesar**). Ratios nuevos verificados en los .md (fuente de verdad): 002=34.5 % @78 SUS; 003=15.7 % @120 Alt-SUS; 007=85.8 % @100 EXP; 008=22.5 % @90 Alt-SUS; 009=26.4 % @60 Alt-SUS; 010=**96.8 % @40 EXP (caso crítico cambió, a 3.2 pts del límite)**; 011=56.1 % @260 Alt-SUS; 012=34.8 % @130 EXP; 013=24.3 % @100 Alt-SUS; 014=55.9 % @70 SUS; 015=36.3 % @80 Alt-SUS. Todas PASSED con admisible TP304L.
- **Hallazgo a verificar en Fase 2**: SIM-007 tiene un caso con admisible 137895.1 kPa (= 20.0 ksi, valor TP304) junto a casos con 115142.4 kPa → resolver antes de publicar su informe.
- **Dos revisiones de inconsistencias con el usuario**: 1.ª revisión encontró StressPercent faltantes (SIM-003/015), archivos de julio, pares "duplicados" (descartado por md5: son vistas distintas), typo "Dezplazamiento", `Grafica/` singular en 11.0, `.md` con espacio y `.md` viejos. El usuario corrigió manualmente; la 2.ª revisión confirmó todo OK salvo: `P2603-PR-SIM-002 .md` (1.0) aún con espacio (lo renombra el agente en Fase 0) y AnexoResultado en espera.
- **Plan redactado (modo plan), NO aprobado para ejecución al cierre**: Fase 0 higiene → Fase 1 herramientas (`scripts/convertir_graficas.py` tif→png con PIL + actualización de `extraer_informe.py` y `parse_caesar_md.py` para leer `Graficas/`) → Fase 2 verificación numérica por línea → Fase 3: 9 informes actualizados + DOCS-189/190 nuevos (SIM-014/015) → Fase 4 dashboard 11 líneas → Fase 5 memoria. Plan completo registrado en `task/todo.md`.
- **Quedó en**: cerrar sesión; **la próxima arranca revisando la carpeta 6.0 (SIM-010) con el usuario** y luego aprobación del plan. Pendientes de siempre: fechas de firma/correo en informes, validación cliente, commit+push, corridas SIM-016 (12.0, tiene .C2) y PCF115 (13.0).

### 2026-09-08
- **Condiciones de operación (P/T) levantadas y archivadas**: los .md CAESAR no traen input echo → presión derivada del SLP impreso (P = 4·SLP/(OD/t)). Tabla por línea guardada en [[20-Lineas/Resumen de líneas#Condiciones de operación]]: T = 90 °C las 8; P2 ≈ 150 psi (≈1034 kPa) en SIM-002/003/007/008/009/010/011; SIM-012 sin presión (gravedad). **Discrepancia detectada**: informes/bases declaran 180 psig → pendiente verificar en los .C2 (registrado en [[60-Pendientes/Pendientes y bloqueos]]).
- **Migración CLAUDE.md → AGENTS.md**: contenido idéntico; 10 referencias actualizadas en 7 archivos (bóveda, skills, `inicializar.md`, `generadorinf.md`, `task/todo.md`); `Plantilla latex/CLAUDE_plantilla.md` intacto. Revisión aplicada: excepción a la fase 1 para tareas con pipeline maestro ya aprobado (p. ej. `generadorinf.md`) + cruce a la bóveda en `cierre_de_sesion`.
- **Informe SIM-013 generado (modo directo, primera aplicación de la excepción)**: DOCS-188, 22 pág., 0 refs indefinidas, PASSED 24.3 % @100 (Alt-SUS) W+P2. Prosa adaptada de SIM-002; peculiaridades y salvedades en [[20-Lineas/SIM-013]]. Dashboard NO actualizado (decisión del usuario: parser después, junto con PCF112).
- **Lección (feedback del usuario)**: en documentos cliente NO se mencionan nombres de archivos internos, salvedades de origen ni jerga de herramienta ("salida deshabilitada", "N/A", "desbordamiento de formato", "archivo de mapeo") en captions ni en la prosa. Barrido completo de los 9 informes (2026-09-08): solo DOCS-188 tenía esos textos (Fig. 5 + §6.1 + §7 + §3 alcance) → corregidos y recompilado (22 pág., 0 refs indefinidas, verificación visual OK). DOCS-180 (SIM-002) ya estaba limpio. Se conservan por ser contenido de ingeniería legítimo: la declaración de advertencias del reductor 130→140, la nota de gráficos no exportados en 008/009/010 y la verificación externa de cargas en boquillas. Siguen pendientes datos del usuario: `\ead{[correo@dmlsas.com]}` y fechas `[DD/MM/AAAA]` en los 9 informes.
- **PCF nuevos corregidos para CAESAR II (2026-09-08)**: aplicado `fix_pcf.py` a `11.0 TUBERIA DE FILTRO DE FIBRAS PP1/PCF114.pcf` (4 END-POSITION-* + 20 pares stub-end/brida + 2 instrumentos longitud cero + 4 TAP-CONNECTION), `12.0 SUCCION BOMBA PP30SR03/PCF116.pcf` (6 END-POSITION-* + 4 pares) y `13.0 SUCCION BOMBA PP30BT17/PCF115.pcf` (6 END-POSITION-* + 4 pares), sin avisos. Respaldos `PCF11X - ORIGINAL PLANT3D.pcf` creados. Material verificado TP304L/WP304L en los 3; **OJO: PCF114 trae un tramo Sch 40S además de Sch 10S** (bases dicen Sch 10S — verificar en la corrida). Listos para importar en CAESAR II 2019.
- **Auditoría de corridas + SIM-003 procesada (2026-09-08, tarde)**: barrido de admisibles en todos los .md/.OUT → SIM-003 tenía .OUT TP304L desde el 2026-09-04 sin procesar; SIM-007 tiene .OUT INCOMPLETO (sin esfuerzos ni compliance → re-exportar); SIM-014 (TMS, PASSED 55.9 % @70 SUS) y SIM-015 (filtro fibras PP1, PASSED 36.3 % @80 Alt-SUS) corridas nuevas del día sin procesar; solo faltan por recorrer SIM-009/010/011. SIM-003 procesada: nuevo `P2603-PR-SIM-003.md` (viejo a `_corrida_anterior_TP304/`), `extraer_informe.py` OK, DOCS-181 recompilado (21 pág., 0 refs indefinidas, PASSED 15.7 % @120 Alt-SUS, admisible 115142.4 kPa; nota neutral de desplazamientos — casos 2/4 sin reporte). Parser/dashboard sigue diferido.
- **Quedó en**: procesar SIM-014 y SIM-015 (parser + informes DOCS-189/190) cuando el usuario lo indique; usuario re-exporta SIM-007 completo y recorre SIM-009/010/011; corridas pendientes de PCF116 (12.0) y PCF115 (13.0); commit+push acumulado.

### 2026-09-07
- **PCF nuevos corregidos para CAESAR II**: aplicado `fix_pcf.py` a `9.0 DESCARGA TANQUE DE NIVELACION/PCF113.pcf` (3 END-POSITION-* + 6 pares stub-end/brida fusionados) y `10.0 DESCARGA TMS/PCF112.pcf` (10 END-POSITION-* + 6 pares + 1 TAP-CONNECTION), sin advertencias. Respaldos creados como `PCF11X - ORIGINAL PLANT3D.pcf` en cada carpeta. Ambos listos para importar/editar en CAESAR II 2019.
- **Quedó en**: el usuario importa y corre PCF113/PCF112 en CAESAR; cuando entregue los .OUT/.md (y defina su codificación SIM), procesar con el pipeline habitual (parser + dashboard + eventual informe). Siguen en pie: recorridas TP304L SIM-003/007/009/010/011, gráficos SIM-008/009/010, fechas de firma, validación cliente, commit+push acumulado.

### 2026-09-04
- **Membrete de informes uniformado a 9pt** (pedido del usuario: "diferentes tamaños se ven mal"; se pidió primero 11pt y luego se ajustó a 9pt). `\headerfont` = `\fontsize{9}{10}` aplicado a TODO el texto del membrete (antes mezclaba `\small`/`\scriptsize`/`\tiny` = 11/8/5pt); jerarquía solo por negrita. Ajustes de layout: arraystretch 0.90, columnas logos 2.2 cm (img 2.0×2.2), etiquetas `m{2.1cm}`, valores `m{3.1cm}` (centrado vertical). Iterado con renders hasta eliminar traslapes de multirow. Detalle técnico en [[30-Decisiones/Decisiones y lecciones]].
- **Corrección de texto (reportar al usuario)**: `\headerlinesubdos` de SIM-010 cambiado a "ANALISIS DE FLEXIBILIDAD SUCCION RECIRCULACION 3 DDW" (era "SUCCION BOMBAS DE RECIRCULACION DDW", inconsistente con 008/009 y con su projecttitle; además desbordaba).
- Propagado a las 9 copias de `config/header.tex` (Plantilla + 8 informes) y recompilados los 8 PDF: 0 refs indefinidas, 0 overfull vbox, mismas páginas (23/21/22/20/22/23/25/22). Verificación visual OK (SIM-002, SIM-008, SIM-010, SIM-012).
- **Renumeración de códigos DOCS-20X → DOCS-18X (instrucción del usuario)**: mismo orden SIM (002→180, 003→181, 007→182, 008→183, 009→184, 010→185, 011→186, 012→187). Editados `\documentcode` (datos_proyecto.tex) y el código citado en `06_alcance.tex` de los 8 informes; recompilados con jobname nuevo (0 refs indefinidas, mismas páginas); PDF viejos DOCS-20X eliminados (en build/ quedan algunos logs viejos, inofensivos). Referencias actualizadas en bóveda, `contexto.md`, `task/todo.md` y `generadorinf.md`. Verificado: membrete DOCS-180 en SIM-002 y 24 ocurrencias de DOCS-187 / 0 de DOCS-207 en el PDF de SIM-012.
- **(misma sesión) Ajuste final del membrete**: el usuario pidió el código `I24.104-...DOCS-18X` en UNA sola línea. Columna de valores 3.1→**4.5 cm** (la central X absorbe); textos siguen todos a 9pt. Propagado a las 9 copias de `header.tex` y recompilados los 8 PDF (0 refs indefinidas, 0 overfull vbox, mismas páginas). Verificación visual OK en SIM-002, SIM-010 y SIM-012.
- **(misma sesión) Rejilla completa portada/hoja de firmas propagada**: los 7 informes restantes compartían `00_portada.tex`/`00_hojafirmas.tex` idénticos (md5, todo por macros) → copia directa desde SIM-002 a los 7 + `Plantilla latex/`. 7 PDF recompilados (0 refs indefinidas, mismas páginas); verificación visual SIM-011/SIM-012 (portada y hoja de firmas con rejilla completa y encabezados verdes, igual que DOCS-180). Con esto queda cerrado el pendiente del 2026-09-03.
- **(misma sesión) Recuento de la recorrida TP304L**: el usuario pidió un cuadro de estado — quedó documentado en [[60-Pendientes/Pendientes y bloqueos]]: faltan 5 recorridas (SIM-003, 007, 009, 010, 011), orden sugerido con SIM-007 de última (saldrá FAILED ~103.9 % EXP y exige decisión de diseño); ratios esperados estimados (×1.198): 003 ~15.7 %, 009 ~24.9 %, 010 ~72.9 %, 011 ~67.2 %. Pipeline por .OUT ya probado con SIM-002.
- **Quedó en (cierre)**: pendiente commit+push (acumula: renombrados, SIM-002 TP304L, membrete 9pt + código en una línea, corrección SIM-010, renumeración DOCS-18X, rejilla completa ×8 + plantilla). Siguen en pie: recorridas SIM-003/007/009/010/011 con TP304L, gráficos SIM-008/009/010, fechas de firma, validación cliente. Memoria sincronizada al cierre: pendientes, log, decisiones, `contexto.md`, `task/todo.md`. Próxima sesión: «ejecuta inicializa» y procesar cada .OUT entregado.

### 2026-09-02 (sesión larga, cerrada por límite de uso)
- SIM-012 (Succión TK Blowtank) completa: PCF111 corregido, análisis PASSED 32.4 % @130 EXP, .md desde .OUT, 3 tif→png, parser multi-gráfico, dashboard 8/8 en producción (commit `db8d3dc`).
- `generadorinf.md` v1.1 creado; informe piloto SIM-012 compilado (DOCS-187, 22 pág.).
- Membrete: rediseño revertido por el usuario; layout original conservado con textos/firmas/logo2 ajustados.
- **Quedó en**: rollout de informes SIM-002…011 y gráficos SIM-008/009/010 pendientes.

### 2026-09-03 (sesión 2, tarde)
- **Verificación de admisibles (pendiente del backlog) — HALLAZGO MAYOR**: 6 modelos (SIM-002/003/007/009/010/011) corridos con admisible TP304 (20.0 ksi) cuando todo el proyecto es TP304L (16.7 ksi; confirmado en los 8 PCF: toda tubería/accesorio BW es A312 TP304L / A403 WP304L). SIM-008 y SIM-012 están correctos. Recalculado: 5 líneas siguen PASSED; **SIM-007 EXP → ~103.9 % FAILED**. Detalle en [[60-Pendientes/Pendientes y bloqueos]]. Decisión del usuario pendiente (recorrer en CAESAR con TP304L).
- **Orden de archivos (usuario)**: carpetas de simulación renombradas con prefijo `1.0`–`8.0` en orden SIM; .C2 renombrados a `P2603-PR-SIM-XXX.C2` (003/007 normalizados con "SIM"; PCF111.C2 → P2603-PR-SIM-012.C2; SIM-010 sin .C2). Parser verificado tras el cambio: 8/8, JSON idéntico. Los .md/.tif/.wrn NO se renombraron (decisión del usuario: solo .C2).
- Instrucción registrada: la hoja de firmas de los informes es la única excepción al formato de tablas (bordes completos) — no tocarla.
- **Quedó en**: esperando decisión sobre admisibles (recorrida CAESAR) y entrega de .OUT nuevos; pendiente commit+push de los renombrados.
- **(misma sesión, continuación)** El usuario decidió recorrer las 6 líneas con TP304L y entregó SIM-002: nuevo `.OUT` (Job Name ya renombrado P2603-PR-SIM-002), admisible 115142.4 kPa ✓, PASSED 34.5 % @78 SUS (EXP 28.0 % @90, admisible 254505.3). Pipeline actualizado: regex de `parse_caesar_md.py` y `extraer_informe.py` aceptan `P2603-PR-(PL-)?(SIM-)?`; detección de isométrico ahora prefiere `PCF*` (los nuevos `Dezplazamiento.png`/`NodosSoporte.png` se colaban como isométrico). Informe DOCS-180 recompilado con números nuevos + 2 figuras nuevas. El viejo .md quedó en `1.0 .../_corrida_anterior_TP304/`.
- **Quedó en (cierre)**: esperando las recorridas de SIM-003, SIM-007, SIM-009, SIM-010, SIM-011 para repetir el mismo pipeline por línea.
- **(cierre de sesión)** Además se aplicó la regla de formato del usuario: tablas de portada y hoja de firmas con rejilla completa (único formato así en el informe) — hecho en SIM-002, falta propagar a los otros 7 informes y a `Plantilla latex/`. Memoria sincronizada al cierre: pendientes, log, resumen de líneas, decisiones, `contexto.md`, `task/todo.md`. Próxima sesión: «ejecuta inicializa» y repetir pipeline por cada .OUT entregado.

### 2026-09-03 (sesión 1, mañana)
- Sesión de recuperación: creada bóveda Obsidian (`boveda/`), skills `inicializar`/`memoria` (`.kimi-code/skills/`) e `inicializar.md` en la raíz para memoria persistente entre sesiones.
- **Rollout de informes COMPLETADO**: los agentes de la sesión anterior (2026-09-02) solo habían hecho extracción + frontmatter; las secciones 03/04–13 estaban en placeholders de plantilla (SIM-009 era copia íntegra de SIM-012 con datos erróneos). Swarm de 7 agentes redactó las secciones faltantes con datos del `.md` y compiló los 7 PDF (DOCS-180…206). Los 8 informes están compilados, 0 refs indefinidas, números verificados contra los `.md`, lectura visual OK.
- Incidencias registradas: `tab_esfuerzos_critico.tex` de SIM-011 convertido a longtable (156 filas); inconsistencia de admisibles Sc=Sh (20.0 ksi en 6 líneas vs 16.7 ksi afirmado en SIM-012) → pendiente de verificación.
- **Quedó en**: 8/8 informes compilados; pendientes de usuario: fechas de firma, gráficos SIM-008/009/010, validación cliente, commit+push.
- Cierre: contexto y memoria guardados — bóveda completa al día (notas de línea, pendientes, decisiones, log), `contexto.md` y `task/todo.md` sincronizados. Próxima sesión: «ejecuta inicializa».
